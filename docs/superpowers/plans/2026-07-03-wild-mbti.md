# 엽기 상황극 MBTI 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox 구문.

**Goal:** `play/mbti.html`을 엽기 상황극 12문항 버전으로 전면 교체하고 메인 카드를 갱신, 검증 후 배포.

**콘텐츠 원전:** `docs/content/wild-mbti-source.md` — 12문항 JSON(4절)·16유형 데이터(5절: 이모지 매핑표 + 유형별 명칭/성격 팩폭/우주적 운명)·계산 로직(6절)을 **글자 그대로** 옮긴다(재작성 금지). 유일한 예외: ISFJ '우주적 운명'은 명칭과 맞게 아래 텍스트로 교체:

> "산소 게이지가 1%로 깜빡이는 구명 우주선에서 '먼저 쉬어, 난 괜찮아'라며 제 산소 호스를 동료 콧구멍에 꽂아주고, 정작 본인은 미소를 유지한 채 가장 먼저 조용히 기절함."

---

### Task 1: play/mbti.html 전면 교체

**Files:** Modify(전체 재작성): `play/mbti.html`

- [ ] **Step 1: 페이지 골격** — 기존 페이지와 동일한 head(Jua, ../css/style.css), 공용 헤더/푸터(common.js data-root=".."), hero: "🍞 엽기 상황극 MBTI" / "뻔한 질문은 가라. 우주적 대위기 12연속, 당신의 찐 본능은?"
- [ ] **Step 2: 인라인 스타일** — 페이지 전용 `<style>`:

```css
.portal { width:150px; height:150px; border-radius:50%; margin:0 auto 18px;
  background: conic-gradient(var(--coral), var(--yellow), var(--mint), var(--sky), var(--purple), var(--coral));
  animation: spin 6s linear infinite; filter: blur(1px); }
.portal:hover { animation-duration: 1.2s; }
@keyframes spin { to { transform: rotate(360deg); } }
.visual-card { font-size:3.4rem; text-align:center; padding:26px 10px; border-radius:18px; margin-bottom:16px; letter-spacing:6px; }
.choice-btn:hover { animation: wiggle 0.3s ease; }
@keyframes wiggle { 25% { transform: rotate(-1.2deg);} 75% { transform: rotate(1.2deg);} }
.copy-q { background:none; border:none; color:var(--ink-soft); font-family:inherit; font-size:0.85rem; cursor:pointer; margin-top:4px; }
```

- [ ] **Step 3: 데이터 임베드** — `QUESTIONS`(원전 4절 JSON 그대로) + 문항별 비주얼 매핑:

| id | emoji | tint |
|----|-------|------|
| 1 | 🛸📻 | tint-purple |
| 2 | 🍞🔥 | tint-coral |
| 3 | 🚀😭 | tint-sky |
| 4 | ☄️🌍 | tint-yellow |
| 5 | 🛗👯 | tint-mint |
| 6 | 🥤👻 | tint-purple |
| 7 | 🌵💀 | tint-mint |
| 8 | 🧟🍳 | tint-coral |
| 9 | 🧟🔦 | tint-sky |
| 10 | 📱💀 | tint-yellow |
| 11 | 📑💢 | tint-coral |
| 12 | ✈️🌧️ | tint-sky |

`RESULTS` 16종: `{emoji, title(별명), memeTitle(명칭), roast(성격 팩폭), fate(우주적 운명)}` — 원전 5절에서 그대로(ISFJ fate만 교체).

- [ ] **Step 4: 로직** — 상태 3개(intro/quiz/result)를 div 전환으로:
  - `calculateMBTI(scores)` 원전 6절 그대로.
  - 진행: 프로그레스 바 갱신, 비주얼 카드, 질문, A/B(.choice-btn), 질문 복사 버튼(`Q…\nA…\nB…\n👉 나도 하기: <URL>` 복사→"복사됨!").
  - 결과 렌더: 이모지·밈타이틀·`유형 (별명)`·팩폭·운명 + 버튼 3개.
  - 해시: `/^#(E|I)(S|N)(T|F)(J|P)$/i` 매칭 시 결과 직행. 결과 표시 시 `history.replaceState`로 해시 기록.
  - 결과 링크 복사: `origin+pathname+"#"+type`.
  - 짤 저장: Canvas 720×900 — 크림 배경(#fff9ec), 상단 5색 띠(팔레트), 중앙 이모지(140px 텍스트), 밈 타이틀(자동 줄바꿈, Jua 폰트 스택), `유형 · 별명`, 하단 "🍞 goblub.vercel.app" — `toBlob` → a[download] 클릭, 실패 시 dataURL 새 탭.
- [ ] **Step 5: 커밋**

### Task 2: 메인 카드 교체 + 검증 + 배포

- [ ] index.html MBTI 카드 → 🍞 "엽기 상황극 MBTI" / "미니 식빵이 된 당신… 12가지 대환장 상황으로 보는 찐 본능"
- [ ] 검증(preview_eval): ① 전부 A → **ESTJ**, 전부 B → **INFP** ② 교차 패턴 1건 수동 대조 ③ `#ENFP` 해시 → 결과 직행, `#ABCD` → 인트로 ④ 질문 복사·링크 복사 문자열 확인 ⑤ 짤 Canvas 생성(toBlob 성공) ⑥ 모바일 375px 가로 스크롤 없음
- [ ] 커밋 → push → 실사이트 200 확인
